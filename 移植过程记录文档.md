# WHEELTEC 机器人文本输入功能移植项目 - 完成报告

## 项目概述

| 项目信息 | 详细内容 |
|---------|---------|
| **项目名称** | WHEELTEC 机器人文本输入控制功能移植 |
| **项目目标** | 将 ROSMASTER M3 机器人的文本输入控制功能移植到 WHEELTEC 机器人 |
| **硬件平台** | WHEELTEC 机器人 + Jetson Orin NX |
| **软件平台** | ROS2 Humble |
| **大模型服务** | 讯飞大模型 |
| **项目状态** | ✅ 已完成 |
| **完成日期** | 2025年11月 |

---

## 一、项目背景

### 1.1 需求来源

用户拥有两款机器人：
1. **WHEELTEC 机器人**：使用 Jetson Orin NX 主控，已有完整的控制程序（雷达建图、视觉巡线等功能）
2. **ROSMASTER M3 机器人**：具备语音及文本输入控制功能

需求是将 ROSMASTER M3 的文本输入控制功能移植到 WHEELTEC 机器人，使用户能够通过文本输入与大模型对话，控制机器人执行各种动作。

### 1.2 项目范围

**已实现功能**：
- ✅ 文本输入界面（text_chat）
- ✅ 与讯飞大模型对话
- ✅ 控制小车底盘运动（前进、后退、转向等）
- ✅ 相机图像识别
- ✅ 视觉跟随功能启动

**不包含功能**：
- ❌ 语音输入功能（用户选择不移植）
- ❌ 语音合成功能（相关代码已注释）
- ❌ 机械臂控制（WHEELTEC 硬件不支持）

---

## 二、移植过程记录

**主要参考文档**：`INTEGRATION_STEPS_CN.md`（位于 `copilot/migrate-rosmaster-control` 分支）

### 2.1 阶段一：系统分析与规划

**时间**：项目初期

**工作内容**：

1. 阅读 `INTEGRATION_STEPS_CN.md` 集成指南，了解完整的移植流程

2. 分析集成架构要求：
   ```
   完整系统组成：
   ├── largemodel          (需要复制 - 包含 action_service, model_service)
   ├── text_chat           (需要复制 - 文本输入接口)
   ├── interfaces          (需要复制 - 自定义消息)
   └── utils               (需要复制 - 工具模块)
   ```

3. 确定移植范围：
   - ✅ 文本输入功能（text_chat）
   - ✅ 大模型服务（model_service）
   - ✅ 动作执行服务（action_service）
   - ❌ 语音功能（用户选择不移植）

**产出文档**：
- `文本输入功能移植分析.md`（22KB）- 详细的技术分析

### 2.2 阶段二：按照 INTEGRATION_STEPS_CN.md 执行移植

**时间**：项目中期

**主要参考**：`INTEGRATION_STEPS_CN.md` 步骤一至步骤五

#### 2.2.1 步骤一：复制核心包

按照集成指南，从 ROSMASTER M3 源码复制必要的包：

```bash
cd ~/wheeltec_ros2/src

# 1. 复制 interfaces 包（动作消息定义）
cp -r /path/to/multimoding/Code/jetson\ nano_RaspberryPi/M3Pro_ws/src/interfaces .

# 2. 复制 text_chat 包（文本输入界面）
cp -r /path/to/multimoding/Code/jetson\ nano_RaspberryPi/M3Pro_ws/src/text_chat .

# 3. 复制 largemodel 包（包含 model_service 和 action_service）
cp -r /path/to/multimoding/Code/jetson\ nano_RaspberryPi/M3Pro_ws/src/largemodel .
```

#### 2.2.2 步骤二：配置包依赖

修改 `~/wheeltec_ros2/src/largemodel/setup.py`，确保入口点正确：

```python
entry_points={
    'console_scripts': [
        'asr = largemodel.asr:main',
        'action_service = largemodel.action_service:main',
        'model_service = largemodel.model_service:main',   
    ],
},
```

#### 2.2.3 步骤三：配置大模型服务

按照指南配置讯飞大模型：

1. 编辑 `config/wheeltec_config.yaml`：
   ```yaml
   model_service:
     ros__parameters:
       regional_setting: "China"
   ```

2. 配置 API 密钥（在 `utils/large_model_interface.py` 中）

#### 2.2.4 步骤四：注释语音功能

由于用户选择不使用语音功能，按照指南建议注释相关代码：

- 注释 `action_service.py` 中的 `voice_synthesis()` 调用
- 跳过语音识别（ASR）配置

#### 2.2.5 步骤五：编译系统

```bash
cd ~/wheeltec_ros2

# 先编译 interfaces 包
colcon build --packages-select interfaces

# 编译其他包
colcon build --packages-select largemodel text_chat

# 加载环境
source install/setup.bash
```

#### 2.2.6 集成架构

实现的系统架构：
```
用户文本输入 → text_chat → /asr → model_service(讯飞大模型) → action_service → 机器人执行
                           ↑                                        ↓
                           └───────── /text_response ────────────────┘
```

**产出文件**：
- 复制并配置的 `largemodel` 包
- 复制的 `text_chat` 包
- 复制的 `interfaces` 包

### 2.3 阶段三：功能调试与问题解决

**时间**：项目中后期

**遇到的问题及解决方案**：

#### 问题 1：小车不移动

**现象**：文本输入后大模型响应正常，但小车不移动

**诊断过程**：
1. 使用 `ros2 topic echo /cmd_vel` 监听速度话题
2. 确认大模型是否正确调用了动作函数

**解决方案**：
- 创建 `TROUBLESHOOTING_CN.md` 故障排除指南
- 提供系统化的6步调试流程

#### 问题 2：model_type AttributeError

**现象**：
```
AttributeError: 'model_interface' object has no attribute 'model_type'
```

**原因**：`action_service` 在执行动作后尝试调用语音合成，但 `model_interface` 初始化不完整

**解决方案**：
- 注释掉 `action_service.py` 中的 `voice_synthesis()` 调用（因为用户不需要语音功能）
- 在 `TROUBLESHOOTING_CN.md` 中添加详细解决方案

#### 问题 3：动作函数不存在

**现象**：
```
机器人反馈:动作函数不存在，无法执行
```

**原因**：尝试使用视觉跟随等高级功能，但 action_service 中没有定义相关函数

**解决方案**：
- 创建 `ADDING_ACTION_FUNCTIONS_CN.md`（19KB）
- 提供完整的函数添加指南和代码示例

### 2.4 阶段四：功能扩展

**时间**：项目后期

**工作内容**：

根据用户需求，编写了添加以下功能的指南：
1. **视觉跟随**：`start_visual_follower()` / `stop_visual_follower()`
2. **视觉巡线**：`start_line_follower()` / `stop_line_follower()`
3. **导航系统**：`start_navigation_system()` / `stop_navigation_system()`

用户按照指南成功添加了视觉跟随功能。

---

## 三、代码修改记录

### 3.1 用户在 WHEELTEC 系统中的修改

#### 3.1.1 action_service.py 修改

**文件位置**：`~/wheeltec_ros2/src/largemodel/largemodel/action_service.py`

**修改内容**：

1. **注释语音合成调用**（第 578 行附近）：
   ```python
   # 注释掉以下语音合成相关代码
   # self.model_client.voice_synthesis(...)
   ```

2. **添加视觉跟随函数**：
   ```python
   def start_visual_follower(self, color="red"):
       cmd = ["ros2", "launch", "simple_follower_ros2", "visual_follower.launch.py"]
       self.visual_follower_process = subprocess.Popen(cmd)
       # ... 错误处理和状态反馈

   def stop_visual_follower(self):
       # 终止视觉跟随进程
   ```

3. **添加反馈消息**（在 `init_language()` 中）：
   ```python
   "start_visual_follower_done": "机器人反馈:已启动视觉跟随功能，正在跟随{color}色物体"
   ```

#### 3.1.2 配置文件修改

**文件**：`~/wheeltec_ros2/src/largemodel/config/wheeltec_config.yaml`

根据实际使用情况配置讯飞大模型参数。

### 3.2 新增文件清单

| 文件名 | 位置 | 说明 |
|-------|------|------|
| `text_chat_wheeltec.py` | `largemodel/` | 文本输入节点 |
| `wheeltec_text_control.launch.py` | `launch/` | 启动文件 |

---

## 四、测试验证

### 4.1 功能测试结果

| 测试项目 | 测试方法 | 结果 |
|---------|---------|------|
| 文本输入发送 | 在 text_chat 输入文字 | ✅ 成功发送到 /asr |
| 大模型对话 | 输入"你好" | ✅ 讯飞大模型正常响应 |
| 底盘运动控制 | 输入"前进1米" | ✅ 小车正常移动 |
| 转向控制 | 输入"左转90度" | ✅ 小车正常转向 |
| 图像识别 | 输入"看看前面有什么" | ✅ 相机正常识别 |
| 视觉跟随 | 输入"开始视觉跟随" | ✅ 功能正常启动 |
| 停止运动 | 输入"停止" | ✅ 小车正常停止 |

### 4.2 系统启动验证

**启动命令**（4个终端）：

```bash
# 终端 1：底盘驱动
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py

# 终端 2：动作服务
ros2 run largemodel action_service --ros-args --params-file src/largemodel/config/wheeltec_config.yaml

# 终端 3：大模型服务
ros2 run largemodel model_service --ros-args --params-file src/largemodel/config/wheeltec_config.yaml

# 终端 4：文本输入
ros2 run text_chat text_chat
```

**验证结果**：系统正常启动并运行。

---

## 五、项目产出物

### 5.1 文档清单

| 文档名称 | 文件大小 | 内容说明 |
|---------|---------|---------|
| `INTEGRATION_STEPS_CN.md` | ~15KB | **主要参考文档**，完整集成步骤指南（copilot/migrate-rosmaster-control 分支）|
| `文本输入功能移植分析.md` | 22KB | 详细的技术分析和移植策略 |
| `系统架构图解.md` | 14KB | 系统架构图、数据流图、时序图 |
| `QUICKSTART_CN.md` | 6KB | 快速开始指南，3步安装 |
| `README.md` | 4KB | 项目说明和导航 |
| `TROUBLESHOOTING_CN.md` | 12KB | 故障排除指南，常见问题解决 |
| `ADDING_ACTION_FUNCTIONS_CN.md` | 19KB | 添加新动作函数的完整指南 |
| `PROJECT_SUMMARY_CN.md` | 10KB | 项目总结 |
| **本文档** | - | 移植过程记录和结项报告 |

### 5.2 代码文件

| 文件名称 | 代码行数 | 功能说明 |
|---------|---------|---------|
| `text_chat_wheeltec.py` | 167 行 | 文本输入 ROS2 节点 |
| `wheeltec_text_control.launch.py` | ~50 行 | 文本控制启动文件 |

### 5.3 总计

- **文档**：约 87KB（7个文档）
- **代码**：约 220 行

---

## 六、经验总结

### 6.1 成功经验

1. **模块化设计**
   - text_chat 节点独立于其他服务，便于集成
   - 使用标准 ROS2 话题通信，易于调试

2. **完善的文档**
   - 提供了详细的技术分析文档
   - 故障排除指南帮助快速定位问题
   - 功能扩展指南便于后续开发

3. **灵活的架构**
   - 文本和语音输入可以共存
   - 动作函数可以按需添加

### 6.2 遇到的挑战

1. **原始代码的兼容性问题**
   - `model_type` 属性未初始化导致的错误
   - 解决方案：注释掉不需要的语音合成功能

2. **功能扩展需求**
   - 用户需要添加视觉跟随等高级功能
   - 解决方案：编写完整的函数添加指南

### 6.3 后续建议

1. **代码维护**
   - 建议将修改后的代码备份
   - 记录所有配置文件的修改

2. **功能扩展**
   - 可以按照 `ADDING_ACTION_FUNCTIONS_CN.md` 继续添加新功能
   - 如视觉巡线、导航等

3. **系统优化**
   - 考虑将多个服务整合到一个 launch 文件
   - 添加系统状态监控

---

## 七、附录

### 附录 A：系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                      WHEELTEC 机器人系统                          │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌────────────────┐     ┌─────────────────┐
│   用户终端    │────▶│  text_chat节点  │────▶│ model_service  │
│  (键盘输入)   │     │  (文本输入界面) │     │  (讯飞大模型)   │
└──────────────┘     └────────────────┘     └─────────────────┘
                            │                        │
                            │ /asr                   │ 函数调用
                            │                        │
                            │                        ▼
                            │                ┌─────────────────┐
                            │                │ action_service  │
                            │                │   (动作执行)     │
                            │                └─────────────────┘
                            │                        │
                            │                        │ /cmd_vel
                            │                        │
                            │                        ▼
                            │                ┌─────────────────┐
                            │                │  WHEELTEC底盘   │
                            │                │   (执行动作)     │
                            │                └─────────────────┘
                            │                        │
                            │ /text_response         │
                            ▼                        │
                     ┌────────────────┐              │
                     │  text_chat节点  │◀─────────────┘
                     │  (显示响应)     │
                     └────────────────┘
```

### 附录 B：话题接口说明

| 话题名称 | 消息类型 | 方向 | 说明 |
|---------|---------|------|------|
| `/asr` | std_msgs/String | 发布 | 用户文本输入 |
| `/text_response` | std_msgs/String | 订阅 | 系统响应消息 |
| `/cmd_vel` | geometry_msgs/Twist | - | 速度控制指令 |
| `/actionstatus` | std_msgs/String | - | 动作执行状态 |

### 附录 C：相关命令速查

```bash
# 查看所有节点
ros2 node list

# 查看所有话题
ros2 topic list

# 监听用户输入
ros2 topic echo /asr

# 监听速度指令
ros2 topic echo /cmd_vel

# 手动测试底盘
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.2}}" --once
```

---

## 八、结论

本项目成功完成了 ROSMASTER M3 机器人文本输入控制功能到 WHEELTEC 机器人的移植。通过本次移植：

1. ✅ 实现了文本输入与讯飞大模型的对话功能
2. ✅ 实现了通过文本命令控制小车底盘运动
3. ✅ 实现了相机图像识别功能
4. ✅ 实现了视觉跟随功能的启动控制
5. ✅ 提供了完善的技术文档和扩展指南

项目产出包括核心代码文件和完整的技术文档体系，为后续的功能扩展和系统维护提供了良好的基础。

---

**文档版本**：1.0  
**编写日期**：2025年11月  
**项目状态**：✅ 已完成结项

---

*本文档由 GitHub Copilot 协助编写，记录了 WHEELTEC 机器人文本输入功能移植的完整过程。*
