# ROSMASTER M3 文本输入功能移植到 WHEELTEC 机器人 - 技术分析文档

## 1. 项目概述

### 1.1 项目目标
将 ROSMASTER M3 机器人（源码位于 `multimoding` 文件夹）上的文本输入控制功能移植到 WHEELTEC 机器人（核心代码位于 `wheeltec_ros2` 文件夹），使用 WHEELTEC 原有的 ROS2 代码控制机器人行动。

### 1.2 相关硬件
- **目标平台**: WHEELTEC 机器人
- **主控**: Jetson Orin NX
- **ROS版本**: ROS2 Humble
- **源参考**: ROSMASTER M3 (支持 Jetson Orin 和 Jetson Nano/树莓派)

## 2. 现有系统架构分析

### 2.1 ROSMASTER M3 文本输入系统

#### 2.1.1 核心组件
**文本输入节点** (`multimoding/Code/Orin/M3Pro_ws/src/text_chat/text_chat/text_chat.py`)

**主要功能**:
- 提供命令行文本输入界面
- 发布用户输入到 `asr` 话题（ASR = Automatic Speech Recognition，模拟语音识别输出）
- 订阅 `text_response` 话题接收机器人反馈
- 提供输入等待动画效果
- 支持 UTF-8 编码和错误处理

**ROS2 话题接口**:
```python
# 发布话题
Publisher: /asr (std_msgs/String) - 发送用户文本输入

# 订阅话题  
Subscriber: /text_response (std_msgs/String) - 接收机器人响应
```

**关键代码特性**:
1. **多线程架构**:
   - 主线程: ROS2 spin
   - 输入线程: 处理用户输入
   - 动画线程: 显示等待动画

2. **用户体验优化**:
   - 实时显示"思考中"动画
   - 超时机制（10秒）
   - 响应后自动清除动画

3. **错误处理**:
   - UTF-8 编码错误容错
   - 键盘中断捕获
   - 终端状态恢复

### 2.2 WHEELTEC 现有控制系统

#### 2.2.1 核心包: `largemodel_wheeltec`

**位置**: `wheeltec_ros2/src/largemodel_wheeltec/`

**包结构**:
```
largemodel_wheeltec/
├── largemodel/
│   ├── __init__.py
│   └── action_service_wheeltec.py  # 动作执行服务
├── config/
│   ├── wheeltec_config.yaml       # 主配置文件
│   └── map_mapping.yaml           # 导航点配置
├── launch/
│   └── wheeltec_voice_control.launch.py  # 启动文件
├── package.xml                     # ROS2 包清单
└── setup.py                        # Python 包配置
```

#### 2.2.2 动作执行服务 (`action_service_wheeltec.py`)

**主要功能**:
- 接收并执行移动控制指令
- 支持导航到预定义目标点
- 提供速度控制接口
- 发布动作执行状态反馈

**ROS2 话题接口**:
```python
# 发布话题
Publisher: /cmd_vel (geometry_msgs/Twist) - 速度控制
Publisher: /actionstatus (std_msgs/String) - 动作状态反馈
Publisher: /interrupt_flag (std_msgs/Bool) - 中断标志

# 订阅话题
Subscriber: /wakeup (std_msgs/Bool) - 唤醒中断信号
```

**支持的动作函数**:
1. **基础移动**:
   - `move_forward(distance)` - 前进
   - `move_backward(distance)` - 后退
   - `turn_left()` - 左转
   - `turn_right()` - 右转
   - `stop()` - 停止

2. **精确控制**:
   - `move_left(angle, angular_speed)` - 左转指定角度
   - `move_right(angle, angular_speed)` - 右转指定角度
   - `set_cmdvel(linear_x, linear_y, angular_z, duration)` - 自定义速度

3. **导航功能**:
   - `navigation(point_name)` - 导航到目标点
   - `get_current_pose()` - 获取当前位置

4. **辅助功能**:
   - `wait(duration)` - 等待
   - `finish_dialogue()` - 结束对话

#### 2.2.3 配置文件分析

**wheeltec_config.yaml 关键参数**:
```yaml
action_service:
  ros__parameters:
    Speed_topic: "/cmd_vel"       # 速度控制话题
    text_chat_mode: false         # 文本模式开关
    language: "zh"                # 语言设置
```

**现状**: 
- `text_chat_mode` 参数已定义，但未实现对应的文本输入节点
- 系统架构预留了文本输入功能的接口

## 3. 系统集成架构

### 3.1 完整的控制流程

```
[用户输入] 
    ↓
[text_chat节点] 
    ↓ 发布到 /asr 话题
    ↓
[大模型服务节点] ← (需要用户自行配置，见course文件夹教程)
    ↓ 解析意图，调用函数
    ↓
[action_service节点]
    ↓ 执行动作
    ↓ 发布到 /cmd_vel
    ↓
[WHEELTEC底盘驱动]
    ↓
[机器人执行动作]
    ↓
    ↓ 状态反馈 /actionstatus
    ↓
[大模型服务节点]
    ↓ 生成回复 /text_response
    ↓
[text_chat节点显示]
```

### 3.2 话题依赖关系

```
text_chat节点 ────pub: /asr────→ 大模型服务节点
       ↑                              ↓
       │                         (函数调用)
       │                              ↓
       └────sub: /text_response─── action_service节点
                                      ↓
                              pub: /actionstatus
                              pub: /cmd_vel
```

## 4. 移植实施方案

### 4.1 需要创建的文件

#### 4.1.1 文本输入节点模块
**文件**: `wheeltec_ros2/src/largemodel_wheeltec/largemodel/text_chat_wheeltec.py`

**设计思路**:
- 基于 M3 的 `text_chat.py` 进行适配
- 保留核心的用户输入和动画功能
- 适配 WHEELTEC 的话题命名规范
- 添加中文提示信息

**代码框架**:
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
WHEELTEC Robot Text Chat Interface
WHEELTEC 机器人文本输入界面
Adapted from ROSMASTER M3 text_chat
"""

import rclpy
from rclpy.node import Node
from std_msgs.msg import String
import threading
import sys
import time
import os
import signal

def reset_terminal():
    """重置终端状态"""
    os.system("stty sane")

class WheeltecTextChatNode(Node):
    def __init__(self):
        super().__init__('wheeltec_text_chat_node')
        
        # 创建发布者：发送用户输入到 asr 话题
        self.asr_publisher = self.create_publisher(String, 'asr', 10)
        
        # 创建订阅者：接收机器人响应
        self.response_subscriber = self.create_subscription(
            String, 'text_response', self.response_callback, 10
        )
        
        # 状态标志
        self.running = True
        self.response_received = threading.Event()
        self.latest_response = None
        self.animation_active = False
        self.first_response = True
        
        # 启动动画线程
        self.animation_thread = threading.Thread(target=self.display_animation)
        self.animation_thread.daemon = True
        self.animation_thread.start()
        
        # 启动输入线程
        self.input_thread = threading.Thread(target=self.handle_user_input)
        self.input_thread.daemon = True
        self.input_thread.start()
        
        self.get_logger().info("WHEELTEC 文本输入节点已启动")
        self.get_logger().info("请输入您的指令，机器人将执行相应的动作")

    def handle_user_input(self):
        """处理用户输入"""
        while self.running and rclpy.ok():
            try:
                # 读取用户输入
                user_input = input("请输入指令 >>> ").strip()
                
                # UTF-8 编码处理
                user_input = user_input.encode(
                    'utf-8', errors='replace'
                ).decode('utf-8', errors='replace')
                
                if not user_input:
                    continue
                
                # 发布用户输入到 asr 话题
                self.asr_publisher.publish(String(data=user_input))
                self.get_logger().info(f"已发送指令: {user_input}")
                
                # 设置等待状态
                self.first_response = True
                self.response_received.clear()
                self.animation_active = True
                
                # 等待响应（超时10秒）
                if self.response_received.wait(timeout=10):
                    self.first_response = False
                else:
                    self.get_logger().warn("响应超时，请检查大模型服务是否正常运行")
                    self.animation_active = False
                    sys.stdout.write('\r' + ' ' * 50 + '\r')
                    
            except UnicodeDecodeError as e:
                self.get_logger().error(f"输入编码错误: {e}")
                continue
            except EOFError:
                break
            except KeyboardInterrupt:
                self.running = False
                reset_terminal()
                break
            except Exception as e:
                self.get_logger().error(f"输入处理异常: {e}")
                continue

    def response_callback(self, msg):
        """接收机器人响应的回调函数"""
        # 显示响应消息
        self.get_logger().info(f"机器人: {msg.data}")
        
        # 首次响应时设置事件
        if self.first_response:
            self.response_received.set()
        
        # 停止动画
        self.animation_active = False
        sys.stdout.write('\r' + ' ' * 50 + '\r')
        sys.stdout.flush()

    def display_animation(self):
        """显示等待动画"""
        animation_chars = ['|', '/', '-', '\\']
        i = 0
        while self.running:
            if self.animation_active:
                sys.stdout.write(
                    f'\r 机器人正在思考中... {animation_chars[i % len(animation_chars)]}'
                )
                sys.stdout.flush()
                time.sleep(0.1)
                i += 1
            else:
                time.sleep(0.1)

def signal_handler(sig, frame):
    """信号处理器"""
    reset_terminal()
    sys.exit(0)

def main(args=None):
    """主函数"""
    signal.signal(signal.SIGINT, signal_handler)
    rclpy.init(args=args)
    node = WheeltecTextChatNode()
    
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.running = False
        node.input_thread.join(timeout=1)
        node.animation_thread.join(timeout=1)
        node.destroy_node()
        rclpy.shutdown()
        reset_terminal()

if __name__ == '__main__':
    main()
```

#### 4.1.2 修改 setup.py
**文件**: `wheeltec_ros2/src/largemodel_wheeltec/setup.py`

**修改内容**: 添加 text_chat 入口点

```python
entry_points={
    'console_scripts': [
        'action_service = largemodel.action_service_wheeltec:main',
        'text_chat = largemodel.text_chat_wheeltec:main',  # 新增
    ],
},
```

#### 4.1.3 创建文本模式启动文件
**文件**: `wheeltec_ros2/src/largemodel_wheeltec/launch/wheeltec_text_control.launch.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
WHEELTEC Text Control Launch File
WHEELTEC 文本控制启动文件
"""

import os
from ament_index_python.packages import get_package_share_directory
from launch import LaunchDescription
from launch_ros.actions import Node

def generate_launch_description():
    # 获取配置文件路径
    pkg_share = get_package_share_directory('largemodel_wheeltec')
    params_file = os.path.join(pkg_share, 'config', 'wheeltec_config.yaml')

    # Action Service 节点
    action_server = Node(
        package='largemodel_wheeltec',
        executable='action_service',
        name='wheeltec_action_service',
        parameters=[params_file],
        output='screen',
        emulate_tty=True,
    )
    
    # Text Chat 节点
    text_chat = Node(
        package='largemodel_wheeltec',
        executable='text_chat',
        name='wheeltec_text_chat',
        output='screen',
        emulate_tty=True,
    )

    return LaunchDescription([
        action_server,
        text_chat,
    ])

if __name__ == '__main__':
    generate_launch_description()
```

### 4.2 配置文件更新

#### 4.2.1 更新 wheeltec_config.yaml
确保 `text_chat_mode` 参数可用（已存在，无需修改）

```yaml
action_service:
  ros__parameters:
    Speed_topic: "/cmd_vel"
    text_chat_mode: true          # 启用文本模式
    language: "zh"                # 中文模式
```

### 4.3 编译和安装

```bash
# 进入工作空间
cd ~/wheeltec_ros2

# 编译包
colcon build --packages-select largemodel_wheeltec

# 加载环境变量
source install/setup.bash
```

## 5. 使用指南

### 5.1 完整启动流程

#### 步骤 1: 启动 WHEELTEC 机器人基础功能
```bash
# 终端 1: 启动机器人底盘
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py
```

#### 步骤 2: 启动大模型服务（参考 course 文件夹教程）
```bash
# 终端 2: 启动大模型服务
# 注意：这部分需要用户根据 course 文件夹中的教程自行配置
# 教程文件：
#   - 1.Semantic understand and command follow.pdf
#   - 5.Configure AI large model.pdf
```

#### 步骤 3: 启动文本输入控制
```bash
# 终端 3: 启动文本控制节点
ros2 launch largemodel_wheeltec wheeltec_text_control.launch.py
```

### 5.2 使用示例

**示例对话**:
```
请输入指令 >>> 前进1米
机器人正在思考中... |
机器人: 机器人反馈:执行move_forward(1.0)完成

请输入指令 >>> 左转90度
机器人正在思考中... /
机器人: 机器人反馈:执行move_left(90,0.5)完成

请输入指令 >>> 导航到point1
机器人正在思考中... -
机器人: 机器人反馈:执行navigation(point1)完成
```

### 5.3 仅测试文本输入节点（不含大模型）
如果只想测试文本输入节点是否工作：

```bash
# 终端 1: 启动文本输入节点
ros2 run largemodel_wheeltec text_chat

# 终端 2: 监听 asr 话题
ros2 topic echo /asr

# 终端 3: 手动发送响应（模拟大模型）
ros2 topic pub /text_response std_msgs/String "data: '测试响应消息'"
```

## 6. 与 ROSMASTER M3 的差异

### 6.1 功能对比

| 功能项 | ROSMASTER M3 | WHEELTEC |
|--------|--------------|----------|
| 文本输入界面 | ✅ | ✅ (移植后) |
| 移动底盘控制 | ✅ | ✅ |
| 导航功能 | ✅ | ✅ |
| 机械臂控制 | ✅ | ❌ (不支持) |
| 物体抓取 | ✅ | ❌ (不支持) |
| AprilTag识别 | ✅ | ❌ (不支持) |
| 视觉识别 | ✅ | ❌ (不支持) |

### 6.2 简化说明

WHEELTEC 版本专注于**移动底盘控制**，移除了：
- 机械臂相关的所有函数
- 视觉识别相关功能
- AprilTag 检测功能
- 物体抓取功能

这些功能的移除是因为 WHEELTEC 机器人硬件不支持这些功能。

## 7. 大模型配置说明

### 7.1 重要提示

**文本输入节点本身不包含大模型功能**。它仅负责：
1. 接收用户文本输入
2. 发布到 `asr` 话题
3. 显示来自 `text_response` 话题的响应

### 7.2 大模型服务的作用

大模型服务是整个系统的"大脑"，负责：
1. 订阅 `/asr` 话题接收用户指令
2. 理解用户意图（自然语言理解）
3. 调用合适的动作函数（function calling）
4. 接收 `/actionstatus` 话题的执行反馈
5. 生成自然语言回复
6. 发布到 `/text_response` 话题

### 7.3 配置参考

用户需要参考 `course` 文件夹中的教程文档：
- **1.Semantic understand and command follow.pdf**: 语义理解和指令跟随
- **5.Configure AI large model.pdf**: AI大模型配置

这些教程（针对国际用户）提供了：
- 如何配置大模型 API
- 如何定义函数调用接口
- 如何实现语义理解

**注意**: 由于这些教程是英文的，中文用户在配置时可能需要：
1. 选择支持中文的大模型服务（如：通义千问、文心一言、ChatGLM 等）
2. 修改 prompt 为中文
3. 确保函数描述使用中文

## 8. 故障排除

### 8.1 文本输入节点相关

#### 问题 1: 节点启动失败
**症状**: `ros2 run largemodel_wheeltec text_chat` 报错

**解决方案**:
```bash
# 检查是否正确编译
cd ~/wheeltec_ros2
colcon build --packages-select largemodel_wheeltec
source install/setup.bash

# 检查入口点是否正确
ros2 pkg executables largemodel_wheeltec
# 应该看到: text_chat
```

#### 问题 2: 输入后无响应
**症状**: 输入指令后一直显示"思考中"动画，超时

**可能原因**:
1. 大模型服务未启动
2. 话题连接问题

**调试步骤**:
```bash
# 检查话题是否存在
ros2 topic list | grep -E "(asr|text_response)"

# 监听 asr 话题，确认输入是否发布
ros2 topic echo /asr

# 手动发送测试响应
ros2 topic pub /text_response std_msgs/String "data: '测试'" --once
```

#### 问题 3: 中文显示乱码
**症状**: 终端中文显示为乱码或问号

**解决方案**:
```bash
# 设置终端编码为 UTF-8
export LANG=zh_CN.UTF-8
export LC_ALL=zh_CN.UTF-8

# 或使用支持 UTF-8 的终端（如 GNOME Terminal）
```

### 8.2 机器人控制相关

#### 问题 4: 机器人不响应控制指令
**症状**: 大模型返回成功，但机器人不移动

**检查步骤**:
```bash
# 1. 确认 /cmd_vel 话题有数据
ros2 topic echo /cmd_vel

# 2. 确认 WHEELTEC 底盘驱动正常
ros2 node list | grep wheeltec

# 3. 手动测试速度控制
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist \
  "{linear: {x: 0.2}, angular: {z: 0.0}}" --once
```

#### 问题 5: 导航功能不工作
**症状**: navigation() 函数调用失败

**检查步骤**:
```bash
# 1. 确认 Nav2 正在运行
ros2 node list | grep nav

# 2. 检查地图是否加载
ros2 topic echo /map --once

# 3. 确认目标点已配置
# 编辑: ~/wheeltec_ros2/install/largemodel_wheeltec/share/largemodel_wheeltec/config/map_mapping.yaml
```

### 8.3 大模型服务相关

#### 问题 6: 大模型无法调用函数
**症状**: 大模型返回文本回复，但不执行动作

**可能原因**:
- 函数定义不正确
- 大模型不支持 function calling
- Prompt 设置问题

**解决建议**:
1. 参考 `course` 文件夹教程正确配置函数调用
2. 使用支持 function calling 的大模型（如 GPT-4, GPT-3.5-turbo）
3. 检查函数描述是否清晰

#### 问题 7: 大模型响应慢
**症状**: 等待时间超过10秒经常超时

**解决方案**:
```python
# 在 text_chat_wheeltec.py 中修改超时时间
if self.response_received.wait(timeout=30):  # 改为30秒
    self.first_response = False
```

## 9. 扩展开发建议

### 9.1 添加语音输入支持

如果需要同时支持语音输入，可以：
1. 添加语音识别（ASR）节点
2. 将识别结果也发布到 `/asr` 话题
3. 保持文本输入节点同时运行

两种输入方式可以共存，都通过 `/asr` 话题与系统交互。

### 9.2 添加 GUI 界面

可以开发图形界面替代命令行：
```python
# 使用 PyQt5 或 Tkinter 创建 GUI
# GUI 节点同样发布到 /asr 话题
# 订阅 /text_response 话题显示响应
```

### 9.3 添加历史记录

```python
class WheeltecTextChatNode(Node):
    def __init__(self):
        super().__init__('wheeltec_text_chat_node')
        self.history = []  # 存储对话历史
        
    def handle_user_input(self):
        # 保存输入
        self.history.append({"role": "user", "content": user_input})
        
    def response_callback(self, msg):
        # 保存响应
        self.history.append({"role": "assistant", "content": msg.data})
```

### 9.4 集成 WHEELTEC 现有功能

将 WHEELTEC 的特色功能集成到动作系统：
```python
# 在 action_service_wheeltec.py 中添加
def start_line_following(self):
    """启动视觉巡线功能"""
    # 调用 WHEELTEC 的巡线包
    pass
    
def start_slam(self):
    """启动 SLAM 建图"""
    # 调用 WHEELTEC 的 SLAM 功能
    pass
```

## 10. 总结

### 10.1 移植要点

1. **核心理念**: 文本输入节点是一个简单的"输入-输出"接口
   - 输入: 用户文本 → `/asr` 话题
   - 输出: `/text_response` 话题 → 用户终端

2. **系统分工**:
   - `text_chat`: 用户界面
   - 大模型服务: 智能决策
   - `action_service`: 动作执行

3. **移植难度**: ⭐⭐☆☆☆ (较简单)
   - 代码量小（约150行）
   - 不涉及复杂的 ROS2 功能
   - 主要是适配话题名称和提示信息

### 10.2 预期效果

移植完成后，用户可以：
1. ✅ 通过命令行输入自然语言指令
2. ✅ 看到机器人实时执行动作
3. ✅ 收到动作完成的反馈
4. ✅ 享受流畅的交互体验

### 10.3 下一步工作

完成文本输入功能移植后，建议：
1. 测试所有基础动作函数
2. 配置常用的导航目标点
3. 根据需要训练或微调大模型
4. 考虑添加语音输入功能

### 10.4 参考资料

- **WHEELTEC 文档**: `wheeltec_ros2/src/largemodel_wheeltec/README_CN.md`
- **大模型配置**: `course/` 文件夹中的 PDF 教程
- **ROS2 文档**: https://docs.ros.org/en/humble/
- **Nav2 文档**: https://navigation.ros.org/

## 11. 附录

### 11.1 完整的话题列表

| 话题名 | 消息类型 | 发布者 | 订阅者 | 说明 |
|--------|----------|--------|--------|------|
| `/asr` | std_msgs/String | text_chat | 大模型服务 | 用户输入文本 |
| `/text_response` | std_msgs/String | 大模型服务 | text_chat | 系统响应 |
| `/actionstatus` | std_msgs/String | action_service | 大模型服务 | 动作执行状态 |
| `/cmd_vel` | geometry_msgs/Twist | action_service | 底盘驱动 | 速度控制 |
| `/wakeup` | std_msgs/Bool | 外部 | action_service | 唤醒打断信号 |
| `/interrupt_flag` | std_msgs/Bool | action_service | 外部 | 打断标志 |

### 11.2 常用指令示例

**移动控制**:
- "前进2米"
- "后退1米"
- "左转90度"
- "右转45度"
- "停止"

**导航控制**:
- "去客厅" (需要在 map_mapping.yaml 中定义)
- "回到原点"
- "去point1"

**查询信息**:
- "当前位置在哪里"
- "获取当前位置"

**组合指令** (需要大模型支持):
- "前进1米然后左转90度"
- "去客厅然后回到原点"

---

**文档版本**: 1.0  
**最后更新**: 2024-11-19  
**适用系统**: WHEELTEC ROS2 Humble on Jetson Orin NX
