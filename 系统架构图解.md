# WHEELTEC 文本输入控制系统架构图解

## 系统整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        WHEELTEC 机器人系统                            │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌────────────────┐     ┌─────────────────┐
│   用户终端    │────▶│  text_chat节点  │────▶│  大模型服务节点  │
│  (键盘输入)   │     │  (文本输入界面) │     │   (AI决策)      │
└──────────────┘     └────────────────┘     └─────────────────┘
                            │                        │
                            │ /asr (String)          │ function_call
                            │                        │
                            │                        ▼
                            │                ┌─────────────────┐
                            │                │ action_service  │
                            │                │   (动作执行)     │
                            │                └─────────────────┘
                            │                        │
                            │                        │ /cmd_vel (Twist)
                            │                        │
                            │                        ▼
                            │                ┌─────────────────┐
                            │                │  WHEELTEC底盘   │
                            │                │   (执行动作)     │
                            │                └─────────────────┘
                            │                        │
                            │                        │ /actionstatus
                            │                        │
                            │                        ▼
                            │                ┌─────────────────┐
                            │                │  大模型服务节点  │
                            │                │  (生成回复)      │
                            │                └─────────────────┘
                            │                        │
                            │ /text_response         │
                            │      (String)          │
                            ▼                        │
                     ┌────────────────┐              │
                     │  text_chat节点  │◀─────────────┘
                     │  (显示响应)     │
                     └────────────────┘
```

## 数据流详解

### 1. 用户输入阶段

```
用户在终端输入: "前进1米"
       ↓
text_chat 节点接收输入
       ↓
发布到 /asr 话题
       ↓
消息内容: {data: "前进1米"}
```

### 2. 意图理解阶段

```
大模型服务订阅 /asr 话题
       ↓
收到消息: "前进1米"
       ↓
自然语言理解 (NLU)
       ↓
解析意图: 执行前进动作，距离=1米
       ↓
函数调用: move_forward(1.0)
```

### 3. 动作执行阶段

```
action_service 接收函数调用
       ↓
执行 move_forward(1.0)
       ↓
计算: 速度=0.3m/s, 时间=1.0/0.3≈3.3秒
       ↓
发布到 /cmd_vel 话题
       ↓
消息内容: {linear: {x: 0.3}, angular: {z: 0.0}}
       ↓
持续发布 3.3 秒
       ↓
WHEELTEC 底盘接收并执行
       ↓
机器人前进 1 米
```

### 4. 反馈阶段

```
action_service 动作完成
       ↓
发布到 /actionstatus 话题
       ↓
消息: "机器人反馈:执行move_forward(1.0)完成"
       ↓
大模型服务接收状态
       ↓
生成自然语言回复
       ↓
发布到 /text_response 话题
       ↓
text_chat 节点接收
       ↓
在终端显示: "机器人回复: 机器人反馈:执行move_forward(1.0)完成"
```

## 话题详细说明

### /asr 话题

```
话题名称: /asr
消息类型: std_msgs/String
发布者: text_chat_wheeltec
订阅者: 大模型服务节点
频率: 按需（用户输入时）
作用: 传递用户的文本输入到AI系统

示例消息:
data: "前进1米"
data: "左转90度"
data: "导航到客厅"
```

### /text_response 话题

```
话题名称: /text_response
消息类型: std_msgs/String
发布者: 大模型服务节点
订阅者: text_chat_wheeltec
频率: 按需（响应时）
作用: 返回AI处理结果到用户界面

示例消息:
data: "机器人反馈:执行move_forward(1.0)完成"
data: "机器人反馈:执行navigation(living_room)完成"
data: "对不起，我没有理解您的指令"
```

### /cmd_vel 话题

```
话题名称: /cmd_vel
消息类型: geometry_msgs/Twist
发布者: action_service_wheeltec
订阅者: WHEELTEC 底盘驱动
频率: 10Hz (动作执行期间)
作用: 发送速度控制指令到底盘

示例消息:
linear: {x: 0.3, y: 0.0, z: 0.0}
angular: {x: 0.0, y: 0.0, z: 0.5}
```

### /actionstatus 话题

```
话题名称: /actionstatus
消息类型: std_msgs/String
发布者: action_service_wheeltec
订阅者: 大模型服务节点
频率: 按需（动作完成时）
作用: 通知AI系统动作执行状态

示例消息:
data: "机器人反馈:执行move_forward(1.0)完成"
data: "机器人反馈:执行navigation(point1)失败"
```

## 节点启动顺序

### 推荐启动顺序

```
1️⃣  WHEELTEC 底盘驱动
    ↓
2️⃣  大模型服务节点
    ↓
3️⃣  action_service + text_chat
```

### 启动命令

```bash
# 步骤 1: 启动底盘 (终端1)
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py

# 步骤 2: 启动大模型服务 (终端2)
# [按照 course 教程配置]

# 步骤 3: 启动文本控制 (终端3)
ros2 launch largemodel_wheeltec wheeltec_text_control.launch.py
```

## 时序图

### 完整交互时序

```
用户       text_chat     大模型服务    action_service    底盘驱动
 │            │             │              │              │
 │  输入"前进" │             │              │              │
 ├───────────▶│             │              │              │
 │            │  /asr       │              │              │
 │            ├────────────▶│              │              │
 │            │             │  调用函数     │              │
 │            │             ├─────────────▶│              │
 │  "思考中.."│             │              │  /cmd_vel    │
 │◀───────────┤             │              ├─────────────▶│
 │            │             │              │              │  执行
 │            │             │              │              ├─────
 │            │             │              │              │
 │            │             │  /actionstatus│             │
 │            │             │◀──────────────┤             │
 │            │  /text_response             │             │
 │            │◀────────────┤               │             │
 │  显示回复   │             │               │             │
 │◀───────────┤             │               │             │
 │            │             │               │             │
```

## 文件结构

### WHEELTEC 包结构

```
wheeltec_ros2/src/largemodel_wheeltec/
├── largemodel/
│   ├── __init__.py
│   ├── action_service_wheeltec.py    # 已有：动作执行服务
│   └── text_chat_wheeltec.py         # 新增：文本输入节点
├── config/
│   ├── wheeltec_config.yaml          # 配置文件
│   └── map_mapping.yaml              # 导航点配置
├── launch/
│   ├── wheeltec_voice_control.launch.py    # 已有：语音控制启动
│   └── wheeltec_text_control.launch.py     # 新增：文本控制启动
├── package.xml
└── setup.py                          # 需要修改：添加入口点
```

## 支持的动作函数

### 基础移动函数

```python
# 前进/后退
move_forward(distance: float)     # 前进指定距离（米）
move_backward(distance: float)    # 后退指定距离（米）

# 转向
turn_left()                        # 左转弯（固定角度）
turn_right()                       # 右转弯（固定角度）
move_left(angle, speed)           # 原地左转指定角度
move_right(angle, speed)          # 原地右转指定角度

# 停止
stop()                            # 立即停止
```

### 导航函数

```python
# 导航到目标点
navigation(point_name: str)       # 导航到预定义点
get_current_pose()                # 获取当前位置
```

### 速度控制函数

```python
# 自定义速度控制
set_cmdvel(linear_x, linear_y, angular_z, duration)
```

### 辅助函数

```python
# 等待
wait(duration: float)             # 等待指定秒数

# 结束
finish_dialogue()                 # 结束当前对话
```

## 配置参数

### wheeltec_config.yaml 关键参数

```yaml
action_service:
  ros__parameters:
    Speed_topic: "/cmd_vel"       # 速度控制话题名
    text_chat_mode: false         # 文本模式开关
    language: "zh"                # 语言: zh/en
```

### map_mapping.yaml 导航点示例

```yaml
living_room:                      # 点名称
  position:
    x: 1.5                        # X 坐标
    y: 2.0                        # Y 坐标
    z: 0.0                        # Z 坐标
  orientation:
    x: 0.0                        # 四元数 X
    y: 0.0                        # 四元数 Y
    z: 0.0                        # 四元数 Z
    w: 1.0                        # 四元数 W
```

## 调试工具

### 查看话题列表

```bash
# 查看所有话题
ros2 topic list

# 查看特定话题
ros2 topic list | grep -E "(asr|text_response|cmd_vel|actionstatus)"
```

### 监听话题内容

```bash
# 监听用户输入
ros2 topic echo /asr

# 监听机器人响应
ros2 topic echo /text_response

# 监听速度指令
ros2 topic echo /cmd_vel

# 监听动作状态
ros2 topic echo /actionstatus
```

### 手动发布消息

```bash
# 模拟用户输入
ros2 topic pub /asr std_msgs/String "data: '前进1米'" --once

# 模拟AI响应
ros2 topic pub /text_response std_msgs/String "data: '测试成功'" --once

# 测试速度控制
ros2 topic pub /cmd_vel geometry_msgs/Twist \
  "{linear: {x: 0.2}, angular: {z: 0.0}}" --once
```

### 查看节点信息

```bash
# 查看所有节点
ros2 node list

# 查看节点详情
ros2 node info /wheeltec_text_chat_node
ros2 node info /wheeltec_action_service
```

## 错误诊断流程图

```
文本输入无响应
      │
      ├─▶ 检查 text_chat 是否运行
      │        ros2 node list | grep text_chat
      │
      ├─▶ 检查 /asr 话题是否发布
      │        ros2 topic echo /asr
      │
      ├─▶ 检查大模型服务是否运行
      │        ros2 node list | grep [模型服务名]
      │
      ├─▶ 检查 /text_response 是否有消息
      │        ros2 topic echo /text_response
      │
      └─▶ 手动测试响应
               ros2 topic pub /text_response std_msgs/String "data: '测试'"


机器人不移动
      │
      ├─▶ 检查 action_service 是否运行
      │        ros2 node list | grep action
      │
      ├─▶ 检查 /cmd_vel 话题是否发布
      │        ros2 topic echo /cmd_vel
      │
      ├─▶ 检查底盘驱动是否运行
      │        ros2 node list | grep wheeltec
      │
      └─▶ 手动测试速度控制
               ros2 topic pub /cmd_vel geometry_msgs/Twist \
                 "{linear: {x: 0.2}}" --once
```

## 性能特性

### 响应时间

```
用户输入 → 显示"思考中" : <100ms
发布到 /asr            : <50ms
大模型处理             : 1-5秒 (取决于模型)
函数调用执行           : 即时
动作完成反馈           : 动作时长 + <100ms
总响应时间             : 约 2-10秒
```

### 资源占用

```
text_chat 节点:
  - CPU: <5%
  - 内存: ~50MB
  - 网络: 忽略不计

action_service 节点:
  - CPU: <10%
  - 内存: ~100MB
  - 网络: 忽略不计
```

## 安全注意事项

⚠️ **使用前请注意**:

1. **测试环境**: 首次使用请在空旷无障碍物的地方测试
2. **速度限制**: 建议初始速度设置较低（0.2-0.3 m/s）
3. **紧急停止**: 可以发送"停止"指令或使用 `/wakeup` 话题
4. **监督使用**: 始终有人监督机器人运行
5. **导航安全**: 确保导航路径上无障碍物

## 总结

本文档提供了 WHEELTEC 文本输入控制系统的完整架构图解。通过理解这些架构图，您可以：

- ✅ 理解系统各组件的关系
- ✅ 掌握数据流向和处理过程
- ✅ 快速定位问题
- ✅ 进行系统扩展和定制

---

**相关文档**:
- 详细分析: `文本输入功能移植分析.md`
- 快速开始: `QUICKSTART_CN.md`
- 实施指南: `wheeltec_text_input_port/README.md`
